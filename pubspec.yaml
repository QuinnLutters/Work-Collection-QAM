name: virtual_buddy_app_design
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_nav_bar: ^5.0.6
  flutter_svg: ^2.0.5
  flutter_staggered_grid_view: ^0.7.0
  fl_chart: ^0.68.0
  percent_indicator: ^4.2.3
  heart_bpm: ^2.0.0+0
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  firebase_core: ^3.1.1
  table_calendar: ^3.1.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/VB-GameBoy.svg
